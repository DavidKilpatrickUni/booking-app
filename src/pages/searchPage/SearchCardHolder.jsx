import React from 'react'

import LongCard from '../../components/LongCard'

const SearchCardHolder = ({ data }) => {
    return (
        <div>
            <LongCard data={data} />
        </div>
    )
}

export default SearchCardHolder
